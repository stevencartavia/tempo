# Rust tempoup

Rust implementation of the Tempo installer. It currently coexists with the
Bash implementation in `tempoup/`; the public bootstrap continues to install
the Bash version until the Rust implementation has completed its staged
migration.
